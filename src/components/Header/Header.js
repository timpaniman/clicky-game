import React from "react";
import "./Header.css";

const Header = props => ( <
    header className = "header" >
    <
    h2 > Memory Game! < /h2> <
    h3 >
    Click on an image to increase points, but don 't click on any more than once! <
    /h3> <
    /header>
);

export default Header;